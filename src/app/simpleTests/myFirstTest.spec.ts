describe('First test', () => {
    it('Should count properly', () => {

        const foo = {
            a: 12,
            b: 34
        };

        const bar = {
            a: 12,
            b: 34
        };

        const a = 'foo';
        const b = 'foo';

        expect(a).toBe(b);
        // expect(foo).toEqual(bar);

    });

});
